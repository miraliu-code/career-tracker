# Postgres connection string.
# On Vercel: create a Postgres database (Storage tab, provisioned via Neon),
# connect it to this project, then run `vercel env pull .env.local` to fetch
# this value locally.
POSTGRES_URL="postgres://user:password@host:5432/database"
